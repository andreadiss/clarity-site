name: Site checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate pages and local resources
        run: node scripts/validate-site.mjs
      - name: Validate JavaScript syntax
        run: |
          node --check js/clarity-animations.js
          node --check js/site-ui.js
